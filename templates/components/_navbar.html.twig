{% block stylesheets %}
	{{ encore_entry_link_tags('navbar') }}
{% endblock %}

<nav class="navbar">
	<div class="container-fluid">
		<a class="navbar-brand bg-light rounded-pill p-2" href="{{ path('welcome') }}">Challenge Yourself <i class="bi bi-keyboard-fill p-2"></i></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('home') }}">
						<i class="bi bi-house-door-fill"></i>
						Challenges</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">
						<i class="bi bi-clock-history"></i>
						Plus récents</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">
						<i class="bi bi-thermometer-high"></i>
						Plus populaires</a>
				</li>
				{% if app.user %}
					<li class="nav-item dropdown">
						<a class="nav-link active dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							<i class="bi bi-person-fill"></i>
							{{ app.user.username }}
						</a>
						<ul class="dropdown-menu ">
							<li>
								<a class="dropdown-item" href="#">
									<i class="bi bi-eye"></i>
									Voir</a>
							</li>
							<li>
								<a class="dropdown-item" href="#">
									<i class="bi bi-wrench-adjustable-circle"></i>
									Créer</a>
							</li>
							<li>
								<a class="dropdown-item" href="#">
									<i class="bi bi-bell"></i>
									Notifications</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_logout') }}">
									<i class="bi bi-x-square-fill"></i>
									Déconnexion</a>
							</li>
						</ul>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link active" href="{{ path('login') }}"> <i class="bi bi-person"></i> Mon compte</a>
					</li>
				{% endif %}

			</ul>
		</div>
	</div>
</nav>
